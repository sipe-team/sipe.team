import { StyledSection } from 'styles/common';
import MainBanner from './MainBanner';

const Section1 = () => {
  return (
    <StyledSection>
      <MainBanner />
    </StyledSection>
  );
};

export default Section1;
